ALTER TABLE CAD_FORNECEDOR
ADD NOM_RUA VARCHAR(60);

ALTER TABLE CAD_FORNECEDOR
ADD NOM_BAIRRO VARCHAR(40);

ALTER TABLE CAD_FORNECEDOR
ADD NUM_CEP NUMERIC(8,0);

ALTER TABLE CAD_FORNECEDOR
ADD COD_CIDADE INTEGER;

alter table CAD_FORNECEDOR
add constraint FK_CAD_CIDADEXCAD_FORNECEDOR
foreign key (COD_CIDADE)
references CAD_CIDADE(COD_CIDADE);

ALTER TABLE CAD_FORNECEDOR
ADD NUM_FONE NUMERIC(10,0);

ALTER TABLE CAD_FORNECEDOR
ADD NUM_FAX NUMERIC(10,0);

ALTER TABLE CAD_FORNECEDOR
ADD NOM_EMAIL VARCHAR(60);

ALTER TABLE CAD_FORNECEDOR
ADD NOM_CONTATO VARCHAR(60);

ALTER TABLE CAD_FORNECEDOR
ADD NOM_GERENTE VARCHAR(60);

update RDB$RELATION_FIELDS set
RDB$NULL_FLAG = NULL
where (RDB$FIELD_NAME = 'COD_CLIENTE') and
(RDB$RELATION_NAME = 'FIS_SAIDA');

